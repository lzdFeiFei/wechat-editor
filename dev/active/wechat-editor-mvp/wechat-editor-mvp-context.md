# 微信公众号排版工具 MVP - 上下文文档

**最后更新：2025-12-30**

---

## SESSION PROGRESS (2025-12-30)

### ✅ 已完成
- PRD 文档分析完成
- 项目需求理解清晰
- 开发计划文档创建完成
- 技术栈选型确定

### 🟡 进行中
- 创建上下文文档和任务清单

### ⏳ 待开始
- 项目初始化
- 技术架构搭建
- 核心功能开发

### ⚠️ 阻塞点
- 无

---

## 项目概览

### 产品定位
一款简洁、免费、易用的微信公众号文章排版工具，面向个人创作者和小型团队。核心特点：
- **零成本**：完全免费，无广告
- **零门槛**：打开即用，无需注册
- **高效率**：10分钟内完成文章排版
- **可定制**：支持保存个人常用样式

### 技术栈
- **前端框架**：React 18 + TypeScript
- **UI 库**：Tailwind CSS + Lucide React
- **富文本编辑器**：Quill.js
- **状态管理**：React Hooks + localStorage
- **构建工具**：Vite
- **部署平台**：Vercel 或 Netlify

---

## 关键文件和目录结构

### 当前项目结构
```
wechat-editor/
├── .claude/                    # Claude Code 配置
│   ├── skills/                 # 自定义技能
│   ├── commands/               # 自定义命令
│   └── hooks.json              # Hooks 配置
├── dev/                        # 开发文档
│   ├── README.md               # Dev Docs 使用指南
│   └── active/                 # 当前任务
│       └── wechat-editor-mvp/  # MVP 开发任务
│           ├── wechat-editor-mvp-plan.md     # 实施计划
│           ├── wechat-editor-mvp-context.md  # 本文件
│           └── wechat-editor-mvp-tasks.md    # 任务清单
├── wechat_editor_prd.md        # 产品需求文档
└── .git/                       # Git 仓库
```

### 未来项目结构（开发后）
```
wechat-editor/
├── src/
│   ├── components/             # React 组件
│   │   ├── Editor/             # 编辑器组件
│   │   ├── Preview/            # 预览组件
│   │   ├── Toolbar/            # 工具栏组件
│   │   ├── Templates/          # 模板组件
│   │   └── common/             # 通用组件
│   ├── hooks/                  # 自定义 Hooks
│   │   ├── useEditor.ts        # 编辑器 Hook
│   │   ├── useDraft.ts         # 草稿 Hook
│   │   └── useTemplate.ts      # 模板 Hook
│   ├── utils/                  # 工具函数
│   │   ├── styleConverter.ts   # 样式转换
│   │   ├── htmlCleaner.ts      # HTML 清理
│   │   ├── imageProcessor.ts   # 图片处理
│   │   └── storage.ts          # 本地存储
│   ├── types/                  # TypeScript 类型定义
│   │   ├── editor.ts           # 编辑器类型
│   │   ├── template.ts         # 模板类型
│   │   └── draft.ts            # 草稿类型
│   ├── styles/                 # 样式文件
│   │   ├── globals.css         # 全局样式
│   │   └── quill-custom.css    # Quill 自定义样式
│   ├── constants/              # 常量定义
│   │   ├── templates.ts        # 预设模板
│   │   ├── colors.ts           # 颜色配置
│   │   └── config.ts           # 应用配置
│   ├── App.tsx                 # 应用入口
│   └── main.tsx                # 主文件
├── public/                     # 静态资源
├── index.html                  # HTML 模板
├── package.json                # 依赖配置
├── tsconfig.json               # TypeScript 配置
├── vite.config.ts              # Vite 配置
├── tailwind.config.js          # Tailwind 配置
└── .eslintrc.js                # ESLint 配置
```

---

## 核心技术决策

### 1. 为什么选择 React + TypeScript？
- **组件化开发**：UI 可以拆分为独立的可复用组件
- **类型安全**：TypeScript 提供编译时类型检查，减少运行时错误
- **生态丰富**：大量成熟的库和工具可用
- **团队熟悉度**：React 是最流行的前端框架

### 2. 为什么选择 Quill.js？
- **轻量级**：核心库体积小，性能好
- **可定制**：支持自定义格式和模块
- **文档完善**：官方文档清晰，社区活跃
- **微信兼容**：可以生成符合微信要求的 HTML

**备选方案**：
- Slate.js：学习曲线陡峭，开发周期长
- Draft.js：Facebook 已不再维护
- TinyMCE/CKEditor：体积大，定制复杂

### 3. 为什么使用 localStorage 而不是后端？
- **降低成本**：无需服务器和数据库
- **隐私保护**：数据存储在用户本地，不上传
- **快速开发**：MVP 阶段无需用户系统
- **离线可用**：无网络也能使用

**未来考虑**：v2.0 可以添加可选的云端同步功能

### 4. 为什么使用内联样式？
- **微信要求**：微信公众号不支持外部 CSS 和 class
- **兼容性好**：内联样式在所有环境都能正常工作
- **复制粘贴**：样式不会丢失

### 5. 为什么选择 Vite 而不是 Create React App？
- **构建速度快**：使用 ESBuild，构建速度是 CRA 的 10-100 倍
- **开发体验好**：HMR（热模块替换）更快
- **配置简单**：开箱即用，配置灵活
- **现代化**：原生支持 ESM，面向未来

---

## 关键技术挑战

### 挑战 1：微信公众号样式兼容性
**问题**：微信公众号对 CSS 支持有限，很多现代 CSS 特性不支持

**支持的样式**：
- color（字体颜色）
- font-size（字号）
- font-weight（加粗）
- text-align（对齐）
- line-height（行高）
- padding、margin（内外边距，部分属性）
- background-color（背景色）
- border（边框）
- border-radius（圆角）

**不支持的样式**：
- position: fixed / sticky
- float（部分情况）
- z-index
- 自定义字体（@font-face）
- CSS 动画
- 复杂的 transform

**解决方案**：
1. 建立测试用例库，覆盖各种样式组合
2. 实现样式转换引擎，过滤不支持的 CSS
3. 使用 table 布局替代某些 flexbox 场景
4. 提供"兼容性检查"功能

### 挑战 2：Quill.js 深度定制
**问题**：Quill.js 默认功能无法满足微信排版需求，需要深度定制

**需要定制的内容**：
- 自定义格式（标题1/2/3、正文、小字）
- 自定义工具栏（颜色选择器、主题切换）
- 特殊组件（分割线、引用框、卡片）
- 样式转换（class → inline style）

**解决方案**：
1. 使用 Quill 的 Parchment 模块创建自定义格式
2. 扩展 Quill 的 Blot 类实现特殊组件
3. 监听 Quill 的 text-change 事件进行样式转换
4. 参考 Quill 官方文档和社区示例

### 挑战 3：图片处理
**问题**：图片需要压缩、格式转换、尺寸适配

**需求**：
- 支持 jpg、png、gif 格式
- 单张图片不超过 5MB
- 自动压缩到适合尺寸（最宽 900px）
- 可选：转换为 base64 或上传到图床

**解决方案**：
1. 使用 Canvas API 进行图片压缩
2. 使用 FileReader API 读取本地文件
3. 实现图片质量和尺寸的平衡算法
4. 可选：集成第三方图床服务（如七牛云、阿里云 OSS）

### 挑战 4：复制功能兼容性
**问题**：不同浏览器对 Clipboard API 支持不一致

**需求**：
- 复制 HTML 格式到剪贴板
- 支持 Chrome、Edge、Safari
- 复制成功率 > 95%

**解决方案**：
1. 优先使用现代 Clipboard API
2. 降级方案：document.execCommand('copy')
3. 测试各浏览器的兼容性
4. 提供手动选择复制的备选方案

---

## 数据结构设计

### 草稿数据结构
```typescript
interface Draft {
  id: string;                 // 唯一标识
  title: string;              // 标题
  content: string;            // HTML 内容
  plainText: string;          // 纯文本（用于搜索）
  createdAt: number;          // 创建时间戳
  updatedAt: number;          // 更新时间戳
  wordCount: number;          // 字数
}

interface DraftStorage {
  drafts: Draft[];            // 草稿列表（最多10篇）
  currentDraftId: string;     // 当前编辑的草稿 ID
}
```

### 模板数据结构
```typescript
interface Template {
  id: string;                 // 唯一标识
  name: string;               // 模板名称
  type: 'preset' | 'custom';  // 预设或自定义
  thumbnail?: string;         // 缩略图（base64）
  styles: {
    heading1: CSSProperties;  // 标题1样式
    heading2: CSSProperties;  // 标题2样式
    heading3: CSSProperties;  // 标题3样式
    paragraph: CSSProperties; // 正文样式
    quote: CSSProperties;     // 引用样式
    code: CSSProperties;      // 代码样式
    theme: {
      primary: string;        // 主色
      secondary: string;      // 辅助色
      text: string;           // 文字色
      background: string;     // 背景色
    };
  };
  createdAt: number;          // 创建时间戳
}

interface TemplateStorage {
  presetTemplates: Template[];  // 预设模板（5套）
  customTemplates: Template[];  // 自定义模板（最多5套）
}
```

### 编辑器状态
```typescript
interface EditorState {
  content: string;            // 当前内容（HTML）
  selection: {                // 当前选区
    index: number;
    length: number;
  };
  history: {                  // 历史记录
    undo: string[];
    redo: string[];
  };
  currentTemplate: string;    // 当前模板 ID
  autoSaveEnabled: boolean;   // 自动保存开关
  lastSaved: number;          // 最后保存时间
}
```

---

## 开发规范

### 代码规范
- **TypeScript 严格模式**：启用所有严格类型检查
- **函数式组件**：使用 React Hooks，不使用 class 组件
- **命名规范**：
  - 组件：PascalCase（如 `EditorToolbar`）
  - 函数/变量：camelCase（如 `handleClick`）
  - 常量：UPPER_SNAKE_CASE（如 `MAX_DRAFT_COUNT`）
  - 类型/接口：PascalCase（如 `DraftStorage`）
- **文件组织**：
  - 一个文件一个组件
  - 相关组件放在同一目录
  - 导出使用 `export default`（组件）或 `export`（工具函数）

### Git 提交规范
使用 Conventional Commits 规范：
- `feat:` 新功能
- `fix:` Bug 修复
- `docs:` 文档更新
- `style:` 代码格式（不影响功能）
- `refactor:` 重构
- `test:` 测试
- `chore:` 构建/工具配置

示例：
```
feat: 实现富文本编辑器基础功能
fix: 修复复制功能在 Safari 中的兼容性问题
docs: 更新 README 使用说明
```

### 组件设计原则
1. **单一职责**：每个组件只负责一个功能
2. **可复用**：组件应该是通用的，可以在不同场景使用
3. **可测试**：组件应该易于测试
4. **性能优化**：使用 React.memo、useMemo、useCallback 避免不必要的重渲染

---

## 快速恢复指南

### 如果上下文重置，如何快速恢复？

1. **阅读本文件**（wechat-editor-mvp-context.md）
   - 查看 SESSION PROGRESS 了解当前进度
   - 查看关键文件和目录结构
   - 查看核心技术决策

2. **阅读任务清单**（wechat-editor-mvp-tasks.md）
   - 查看已完成的任务
   - 查看当前进行中的任务
   - 查看下一步要做的任务

3. **阅读实施计划**（wechat-editor-mvp-plan.md）
   - 了解整体开发策略
   - 查看详细的任务分解
   - 查看风险评估和缓解策略

4. **开始工作**
   - 从任务清单中选择下一个任务
   - 参考实施计划了解任务详情
   - 更新 SESSION PROGRESS 记录进度

### 下一步工作（当前阶段）

**立即开始**：
1. 初始化项目（使用 Vite 创建 React + TypeScript 项目）
2. 配置 Tailwind CSS
3. 安装核心依赖（Quill.js, Lucide React, clipboard.js）
4. 创建基础目录结构

**命令参考**：
```bash
# 创建项目
npm create vite@latest . -- --template react-ts

# 安装依赖
npm install

# 安装 Tailwind CSS
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

# 安装核心库
npm install quill react-quill lucide-react

# 安装工具库
npm install clipboard

# 启动开发服务器
npm run dev
```

---

## 重要提醒

### ⚠️ 关键注意事项

1. **微信样式兼容性**：所有样式必须转为内联，不能使用 class
2. **图片处理**：图片必须压缩到合适大小，避免加载慢
3. **浏览器兼容性**：测试 Chrome、Edge、Safari 三大浏览器
4. **性能优化**：长文章编辑时注意性能，使用防抖和节流
5. **用户体验**：新手引导和错误提示要清晰友好

### 📝 开发过程中要记录的内容

1. **遇到的技术问题和解决方案**
2. **做出的重要技术决策和原因**
3. **发现的新需求或功能调整**
4. **性能优化的具体措施**
5. **兼容性问题和解决方法**

### 🔄 定期更新

- **每完成一个阶段**：更新 SESSION PROGRESS
- **每做出重要决策**：记录到本文件
- **每发现新问题**：添加到阻塞点
- **每天结束时**：更新任务清单

---

## 参考资源

### 官方文档
- **React**：https://react.dev/
- **TypeScript**：https://www.typescriptlang.org/
- **Vite**：https://vitejs.dev/
- **Tailwind CSS**：https://tailwindcss.com/
- **Quill.js**：https://quilljs.com/

### 社区资源
- **React Quill**：https://github.com/zenoamaro/react-quill
- **Quill 自定义格式**：https://quilljs.com/guides/cloning-medium-with-parchment/
- **微信公众号样式规范**：需要实际测试总结

### 竞品参考
- **秀米**：https://xiumi.us/
- **135编辑器**：https://www.135editor.com/
- **新榜编辑器**：https://edit.newrank.cn/

---

**文档版本**：v1.0
**创建日期**：2025-12-30
**最后更新**：2025-12-30
